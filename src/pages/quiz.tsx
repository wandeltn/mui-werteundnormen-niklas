// src/pages/Home.tsx

import React from "react";
import {
    Box
} from "@mui/material";
import theme from "../theme";

function Quiz() {
    return (
        <Box>

        </Box>
    );
};

export default Quiz;